# Core ML
torch>=2.0.0
torchvision>=0.15.0
ultralytics>=8.0.200

# YOLO-NAS (optional — for notebook 03)
# super-gradients==3.7.1   # pip install super-gradients==3.7.1

# SAM — Segment Anything (optional — for notebook 05)
# segment-anything         # pip install 'git+https://github.com/facebookresearch/segment-anything.git'

# SAHI — small object tiling inference (optional — for notebook 07)
# sahi                     # pip install sahi

# Data handling
numpy>=1.24.0
pandas>=2.0.0
Pillow>=9.5.0
opencv-python>=4.8.0
pycocotools>=2.0.6

# Augmentation
albumentations>=1.3.1

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Utilities
PyYAML>=6.0
tqdm>=4.65.0
scikit-learn>=1.3.0
